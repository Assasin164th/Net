<Window x:Class="FishFirm164.TripEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование выхода" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#2d2d2d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Катер:" Grid.Row="0" Grid.Column="0" Margin="5" VerticalAlignment="Center" Foreground="White"/>
        <ComboBox x:Name="cmbBoat" Grid.Row="0" Grid.Column="1" Margin="5" DisplayMemberPath="BoatName" SelectedValuePath="BoatID"/>

        <TextBlock Text="Дата выхода:" Grid.Row="1" Grid.Column="0" Margin="5" VerticalAlignment="Center" Foreground="White"/>
        <DatePicker x:Name="dpDepartureDate" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Дата возвращения:" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center" Foreground="White"/>
        <DatePicker x:Name="dpReturnDate" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Команда:" Grid.Row="3" Grid.Column="0" Margin="5" VerticalAlignment="Top" Foreground="White"/>
        <ListBox x:Name="lbCrew" Grid.Row="3" Grid.Column="1" Margin="5" Height="100" DisplayMemberPath="FullName" SelectionMode="Multiple"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5,20,5,5">
            <Button Content="Сохранить" Width="80" Margin="0,0,10,0" Click="BtnSave_Click"/>
            <Button Content="Отмена" Width="80" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>