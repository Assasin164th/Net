<Window x:Class="Library.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        mc:Ignorable="d"
        Title="Авторизация" Height="380" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FFEDEDED">
    <Grid Margin="15" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Source="/Resources/logo.png" Width="200" Height="50" 
               HorizontalAlignment="Center" Margin="0,0,0,10" Grid.Row="0"/>

        <TextBlock Text="Вход в систему" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,15" 
                   Foreground="Black" Grid.Row="1"/>

        <StackPanel Grid.Row="2" Margin="0,5">
            <TextBlock Text="Логин:" FontWeight="SemiBold" Margin="10,0,0,0"/>
            <TextBox x:Name="txtLogin" Margin="10,5,10,10" Height="30"
                     Background="#FFE0E0E0" BorderBrush="#555" BorderThickness="1"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,5">
            <TextBlock Text="Пароль:" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0"/>
            <TextBox x:Name="txtPassword" Margin="10,5,10,10" Height="30"
                     Background="#FFE0E0E0" BorderBrush="#555" BorderThickness="1"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15">
            <Button x:Name="btnLogin" Content="Войти" Width="100" Height="35" Margin="0,0,15,0"
                    Background="#FFD8D8D8" Foreground="Black" BorderBrush="#555" BorderThickness="1"
                    Click="BtnLogin_Click"/>
            <Button x:Name="btnGuest" Content="Гость" Width="100" Height="35"
                    Background="#FFD8D8D8" Foreground="Black" BorderBrush="#555" BorderThickness="1"
                    Click="BtnGuest_Click"/>
        </StackPanel>

        <TextBlock x:Name="txtError" Grid.Row="5" Text="" Foreground="Red" Margin="0,10"/>
    </Grid>
</Window>
